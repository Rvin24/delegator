{
  "name": "delegator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sponsored EIP-7702 delegate-only tooling for Base. Re-points a compromised EOA's delegation without performing any claim or sweep.",
  "scripts": {
    "delegate": "tsx src/delegate.ts",
    "inspect": "tsx src/inspect.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
